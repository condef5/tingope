<% if notice.present? %>
  <p class="text-center w-full py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium inline-block" id="notice"><%= notice %></p>
<% end %>

<% if alert.present? %>
  <p class="text-center w-full py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium inline-block" id="notice"><%= notice %></p>
<% end %>
